<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Parent: Theme.MaterialComponents.DayNight.NoActionBar
        - AppCompat-derived (required by ComponentActivity + SplashScreen API)
        - No action bar (Compose handles all UI via Scaffold)
        - DayNight: respects system dark/light mode at XML level
        - All actual theming (colors, typography) is handled in Compose MaterialTheme
    -->
    <style name="Theme.IRIS" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>